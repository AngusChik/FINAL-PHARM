# Virtual environment
env/
venv/
.venv/

# Python bytecode
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Database
db.sqlite3
db.sqlite3-journal

# OS files
.DS_Store
Thumbs.db
~$*

# IDE
.vscode/
.idea/

# Claude
.claude/

# Environment secrets
.env

# Collected static files
staticfiles/

# Caddy runtime artifacts
caddy_access.log
caddy_data/

# Runtime logs
logs/

# Google Sheets sync — service-account key (SECRET) and runtime state
google_credentials.json
gsheet_sync_state.json

# McKesson ordering tool — browser profile (LOGIN COOKIES), debug snapshots
# of logged-in pages, and per-run outputs. Never commit these.
.mckesson_profile/
mckesson_debug_*.html
mckesson_order_status.json
mckesson_order_items.json
mckesson_order_report.csv

# Kohl & Frisch ordering tool — same rules: browser profile (LOGIN COOKIES),
# debug snapshots of logged-in pages, and per-run report. Never commit these.
.kohlfrisch_profile/
kf_debug_*.html
kohlfrisch_order_status.json
kohlfrisch_order_items.json
kohlfrisch_order_report.csv
